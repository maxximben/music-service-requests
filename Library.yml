info:
  name: Library
  type: http
  seq: 12

http:
  method: GET
  url: "{{localhost}}/library?userId=1"
  params:
    - name: userId
      value: "1"
      type: query
  auth:
    type: bearer
    token: qwerrtty

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
