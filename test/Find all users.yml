info:
  name: Find all users
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/findAll"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
