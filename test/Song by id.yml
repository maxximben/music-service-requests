info:
  name: Song by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/get-song-by-id?id=1"
  params:
    - name: id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
