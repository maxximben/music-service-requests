info:
  name: Sign up
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/sign-up"
  body:
    type: json
    data: |-
      {
        "email": "vasya2@gmail.com",
        "username": "vasya",
        "password": "Password1."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
