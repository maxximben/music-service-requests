info:
  name: Similar songs (Titles)
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/test/similar-songs/titles/15"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
