info:
  name: Analyze audio
  type: http
  seq: 19

http:
  method: POST
  url: "{{base_url}}/songs/3/analyze"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
