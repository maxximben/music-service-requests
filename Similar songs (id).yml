info:
  name: Similar songs (id)
  type: http
  seq: 10

http:
  method: GET
  url: "{{localhost}}/test/similar-songs/ids/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
