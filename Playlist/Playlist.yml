info:
  name: Playlist
  type: http
  seq: 16

http:
  method: GET
  url: "{{base_url}}/playlist?playlistId=2"
  params:
    - name: playlistId
      value: "2"
      type: query
  auth:
    type: bearer
    token: token123

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
