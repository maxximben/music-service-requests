info:
  name: Create playlist
  type: http
  seq: 4

http:
  method: POST
  url: "{{localhost}}/playlist/create"
  body:
    type: json
    data: |-
      {
        "title": "testPlaylist1",
        "userId": 53
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
