info:
  name: Set title
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{localhost}}/playlist/set-title?title=new_title&playlistId=6"
  params:
    - name: title
      value: new_title
      type: query
    - name: playlistId
      value: "6"
      type: query
  auth:
    type: bearer
    token: "123"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
