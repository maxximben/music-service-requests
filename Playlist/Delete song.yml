info:
  name: Delete song
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{localhost}}/playlist/delete-song?songId=3&playlistId=6"
  params:
    - name: songId
      value: "3"
      type: query
    - name: playlistId
      value: "6"
      type: query
  auth:
    type: bearer
    token: token123

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
