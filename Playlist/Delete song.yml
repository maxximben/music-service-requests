info:
  name: Delete song
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/playlist/delete-song?songId=19&playlistId=2"
  params:
    - name: songId
      value: "19"
      type: query
    - name: playlistId
      value: "2"
      type: query
  auth:
    type: bearer
    token: token123

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
