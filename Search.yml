info:
  name: Search
  type: http
  seq: 10

http:
  method: GET
  url: "{{localhost}}/search?request=q"
  params:
    - name: request
      value: q
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
