info:
  name: Sign in
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/sign-in"
  body:
    type: json
    data: |-
      {
        "email": "vasya@gmail.com",
        "password": "Password1."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
