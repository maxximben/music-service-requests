info:
  name: Get Id by email
  type: http
  seq: 14

http:
  method: GET
  url: "{{localhost}}/get-id-by-email?email=vasya@gmail.com"
  params:
    - name: email
      value: vasya@gmail.com
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
